# Hermes WebUI - Environment Variables
# Copy this file to .env and customize as needed.
# Most configuration lives in ~/.hermes/config.yaml and ~/.hermes/.env (host-level).

# Server port (default: 3001)
# PORT=3001
